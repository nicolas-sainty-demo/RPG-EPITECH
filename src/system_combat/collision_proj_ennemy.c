/*
** EPITECH PROJECT, 2021
** B-MUL-200-BDX-2-1-myrpg-louis.pavlovsky
** File description:
** collision_proj_ennemy
*/

#include <stdbool.h>
#include "window_struc.h"
#include "scene.h"
#include "projectile.h"
#include "collision.h"

bool is_collision_proj_ennemy(the_window *window)
{
    for (int i = 0; window->scene->player->proj[i]; i++) {
        // for (int j = 0; window->scene->enemy[j]; j++) {
        //     if (check_if_collision_btw_circle_square(RADIUS_PROJ\
        //     , window->scene->player->proj[i], window->scene->enemy[j])) {
        //         // printf("collision\n");
        //     }
        // }
        
    }
    ;
}